# --- App ---
APP_NAME=Acme
APP_URL=http://old.test          # set: should overwrite this
APP_ENV=local                    # untouched: should survive

PORT=3000                        # set: should overwrite with the allocated port

# --- Session ---
SESSION_SECURE_COOKIE=true       # ensure: already set, must stay "true"

# --- Unrelated ---
MAIL_MAILER=log                  # untouched: should survive